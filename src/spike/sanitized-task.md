# Sanitized Claude Chat Spike Task

This fixture is intentionally synthetic. It contains no credentials, customer
data, repository secrets, or production instructions.

Use the marker `CLAUDE_SPIKE_TASK_A` for the first disposable task and
`CLAUDE_SPIKE_TASK_B` for the second. The expected observation is that a
conversation resumed for one task never contains the other task's marker.