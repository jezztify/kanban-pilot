{
  "name": "kanban-pilot-workspace-registry",
  "private": true,
  "version": "0.1.0",
  "description": "Discovery registry for Kanban Pilot workspace boards",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && node --test dist/test/*.test.js"
  }
}
